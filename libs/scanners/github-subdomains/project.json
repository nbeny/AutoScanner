{
  "name": "scanners-github-subdomains",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/scanners/github-subdomains/src",
  "projectType": "library",
  "tags": ["scope:scanner"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/libs/scanners/github-subdomains"],
      "options": {
        "jestConfig": "libs/scanners/github-subdomains/jest.config.ts"
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p libs/scanners/github-subdomains/tsconfig.lib.json"
      }
    }
  }
}
