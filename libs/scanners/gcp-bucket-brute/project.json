{
  "name": "scanners-gcp-bucket-brute",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/scanners/gcp-bucket-brute/src",
  "projectType": "library",
  "tags": ["scope:scanner"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/libs/scanners/gcp-bucket-brute"],
      "options": { "jestConfig": "libs/scanners/gcp-bucket-brute/jest.config.ts" }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit -p libs/scanners/gcp-bucket-brute/tsconfig.lib.json" }
    }
  }
}
