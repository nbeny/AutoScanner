{
  "name": "scanners-kubeletctl",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/scanners/kubeletctl/src",
  "projectType": "library",
  "tags": ["scope:scanner"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/libs/scanners/kubeletctl"],
      "options": {
        "jestConfig": "libs/scanners/kubeletctl/jest.config.ts"
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p libs/scanners/kubeletctl/tsconfig.lib.json"
      }
    }
  }
}
