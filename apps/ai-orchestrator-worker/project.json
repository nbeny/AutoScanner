{
  "name": "ai-orchestrator-worker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/ai-orchestrator-worker/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": ["node-env=production"],
        "cwd": "apps/ai-orchestrator-worker"
      },
      "configurations": {
        "development": {
          "args": ["node-env=development"]
        }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "ai-orchestrator-worker:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "ai-orchestrator-worker:build:development"
        },
        "production": {
          "buildTarget": "ai-orchestrator-worker:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/ai-orchestrator-worker"],
      "options": {
        "jestConfig": "apps/ai-orchestrator-worker/jest.config.ts"
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p apps/ai-orchestrator-worker/tsconfig.app.json --noEmit"
      }
    }
  },
  "tags": ["scope:app"]
}
